spring:
  cloud:
    config:
      import: ${sampleapp.spring.cloud.config.uri}

---
spring:
  config:
    activate:
      on-profile: local
sampleapp:
  spring:
    cloud:
      config:
        uri: optional:configserver:http://localhost:2001